[package]
name = "swc-plugin-silk"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "SWC plugin for Silk zero-runtime CSS-in-TypeScript"
repository = "https://github.com/sylphxltd/silk"
authors = ["SylphX Ltd"]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
swc_core = { version = "0.106.16", features = [
  "ecma_ast",
  "ecma_visit",
  "ecma_plugin_transform",
  "ecma_utils",
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
swc_ecma_transforms_testing = "0.106.16"
testing = "0.42.0"

[profile.release]
lto = true
opt-level = 3
